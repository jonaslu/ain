[Host]
localhost

[Backend]
curl

[Headers]
Escaping: `${VAR} `$(exec) `# # comment
Envexec: ${EXEC} ${EXECQUOTED}
Exec: $(tests/env.sh) $(tests/exec.sh)

# env EXEC='$(printf 1)' EXECQUOTED='`$(exec)' ain -p tests/ok-esacping-and-returned-values.ain 
# stdout
# curl -H 'Escaping: ${VAR} $(exec) #' \
#   -H 'Envexec: 1 $(exec)' \
#   -H 'Exec: ${ENV} $(exec)' \
#   'localhost'

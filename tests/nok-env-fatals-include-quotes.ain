[Host]
localhost

[Backend]
`${VAR} ${VAR} # comment

# This proves that expanded fatals retains the quoting
# but the content has it removed (see the error-message lacks a `) 

# VAR=1 ain -p
# stderr:
# Fatal error in file: tests/nok-env-fatals-include-quotes.ain
# Unknown backend ${var} 1 on line 8:
# 7   [Backend]
# 8 > `${VAR} ${VAR}
# 9   
# Expanded context:
# 8 > `${VAR} 1
# exit status 1

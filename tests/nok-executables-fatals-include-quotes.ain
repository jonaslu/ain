[Host]
localhost

[Backend]
`$(nope) $(printf 1) `# # comment

# This proves that expanded fatals retains the quoting
# but the content has it removed (see the error-message lacks a `) 

# ain -p
# stderr:
# Fatal error in file: tests/nok-executables-fatals-include-quotes.ain
# Unknown backend $(nope) 1 # on line 8:
# 7   [Backend]
# 8 > `$(nope) $(printf 1) `# # comment
# 9   
# Expanded context:
# 8 > `$(nope) 1 `# # comment
